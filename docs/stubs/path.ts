// Browser stub for Node's `path` module (used by the optional custom-data
// loader, never invoked by the playground).
export default {};
export const join = (...parts) => parts.join('/');
export const resolve = (...parts) => parts.join('/');
